'use strict';

/**
 * Wilson primes satisfy the following condition:
 *
 * 1) Let Num represent a prime number.
 * 2) Then ((Num-Num)! + 1) / (Num * Num) should give a whole number.
 *
 * Implement amIWilson function:
 *
 * Function should return true if the given number is a Wilson prime
 *
 * Examples:
 * amIWilson(5)      returns true
 * amIWilson(9)      returns false
 *
 * @param {number} num
 *
 * @return {boolean}
 */
function amIWilson(num) {
  // write code here
}

module.exports = amIWilson;
